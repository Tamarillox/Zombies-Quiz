import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tamarillox.zombiesquiz',
  appName: 'Zombies Quiz',
  webDir: 'dist'
};

export default config;
