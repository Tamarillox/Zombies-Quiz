import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zombies.quiz', // Angepasst an package.json
  appName: 'Zombies Quiz',
  webDir: 'dist'
};

export default config;